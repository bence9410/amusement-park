version: "3.8"
services:
  amusement-park:
    image: bence/amusement-park
    ports:
      - 8080:8080
    deploy:
      replicas: 2
  postgres:
    image: bence/postgres-performance-test
  db-admin:
    image: dpage/pgadmin4
    ports:
      - 8079:80
    environment:
      - PGADMIN_DEFAULT_EMAIL=nembence1994@gmail.com
      - PGADMIN_DEFAULT_PASSWORD=admin